version: "3.8"

services:
  mariadb:
    build: code/database/mariaDB/.
    image: mariadb:push-pull
    environment:
      MYSQL_ROOT_PASSWORD: cc-pushpull
    ports:
      - "3306"
  
  database:
    build: code/database/.
    image: logicadb:1.0.0
    environment:
      DB_PASSWORD: 'cc-pushpull'
      DB_HOST: mariadb
    depends_on:
      - mariadb
    ports:
      - "3001"
  
  natssrv:
    image: nats
    ports:
      - "4222"
    command: -js
  
  worker:
    build: code/worker/.
    environment:
        NATS_ENDPOINT: natssrv:4222
        DB_ENDPOINT: database:3001
    depends_on:
      - natssrv
      - database
  
  frontend:
    build: code/frontend/.
    environment:
        NATS_URL: natssrv:4222
        URL: database:3001
    depends_on:
      - natssrv
      - worker
      - database
    ports:
      - "3000:3000"

    
